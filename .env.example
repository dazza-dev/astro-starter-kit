# WordPress GraphQL Endpoint (required, server-side only)
WORDPRESS_GRAPHQL_URL=http://localhost/graphql

# Comma-separated image domains for astro:assets optimization (build-time)
WORDPRESS_IMAGE_DOMAINS=api.example.com,www.example.com
